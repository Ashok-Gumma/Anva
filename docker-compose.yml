version: "3.8"

services:
  anva-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anva-app
    restart: unless-stopped
    ports:
      - "5001:5001"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=production
      - PORT=5001
